<Project Sdk="Microsoft.NET.Sdk">
  <!-- Framework packages, IsTestProject and OutputType all come from tests/Directory.Build.props. -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\ScopedEditors.Abstractions\ScopedEditors.Abstractions.csproj" />
    <ProjectReference Include="..\..\src\ScopedEditors.ViewModels\ScopedEditors.ViewModels.csproj" />
    <ProjectReference Include="..\..\src\ScopedEditors.AvaloniaUI\ScopedEditors.AvaloniaUI.csproj" />
  </ItemGroup>

  <!-- The ported Avalonia tests run against a real, headless Avalonia application. Each fixture
       reaches it through HeadlessUnitTestSession.GetOrStartForAssembly, and with no isolation
       attribute every dispatch builds a fresh one; see Headless/HeadlessSessionTests.cs. -->
  <ItemGroup>
    <PackageReference Include="Avalonia.Headless" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Bennewitz.Ninja.AssemblyQuality" />
    <PackageReference Include="Bennewitz.Ninja.XamlQuality" />
  </ItemGroup>
</Project>
